import React from 'react'
import {  const result = await request(graphqlAPI, query, { slug, categories })

return result.posts} from ''

const Categories = () => {
  return (
    <div>Categories</div>
  )
}

export default Categories